# Journal Cover Letter Skill v2.1 (Recommended stable)

Version 2.1 keeps the v2.0 Research story route and redesigns Review/Synthesis around field diagnosis, synthesis intervention, an editor-facing thesis, controlled uplift, current journal conversation, and adaptive length.

v2.1 保留 v2.0 的 Research 故事路线，并将 Review/Synthesis 重构为领域诊断、综合干预、面向编辑的核心命题、受控营销、当前期刊对话与自适应长度。

Every release asset is built from the matching Git tag. Verify downloads with the attached `SHA256SUMS.txt`.

每个发布包都从对应 Git 标签构建，请使用随附的 `SHA256SUMS.txt` 校验下载文件。
