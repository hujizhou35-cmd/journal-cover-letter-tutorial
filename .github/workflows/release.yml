name: Build release packages

on:
  push:
    tags:
      - "v1.0"
      - "v2.0"
      - "v2.1"
      - "v2.2"
      - "v2.3"
      - "v3.0"
      - "v3.1"
  workflow_dispatch:
    inputs:
      release_ref:
        description: "Public release label"
        required: true
        type: choice
        options: ["v1.0", "v2.0", "v2.1", "v2.2", "v2.3", "v3.0", "v3.1"]

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Select release
        id: release
        shell: bash
        run: echo "ref=${{ inputs.release_ref || github.ref_name }}" >> "$GITHUB_OUTPUT"
      - run: python development/scripts/build_release.py --ref "${{ steps.release.outputs.ref }}"
      - run: python development/scripts/privacy_scan_history.py --archives dist
      - uses: actions/upload-artifact@v4
        with:
          name: journal-cover-letter-${{ steps.release.outputs.ref }}
          path: |
            dist/journal-cover-letter-skill-${{ steps.release.outputs.ref }}.skill
            dist/journal-cover-letter-plugin-${{ steps.release.outputs.ref }}.zip
            dist/SKILL.md
